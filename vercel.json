{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm run build && cp -r dist/public/* dist",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}

