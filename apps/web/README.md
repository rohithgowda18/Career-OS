# Frontend

This folder contains all frontend-related code and assets for the Event App Tracker project.

## Structure

- `src/` - Source code (React components, hooks, pages, etc.)
- `public/` - Static assets and manifest
- `dev-dist/` and `dist/` - Build output directories
- `index.html`, `vite.config.ts`, `vercel.json` - Project configuration files
