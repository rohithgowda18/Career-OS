server:
  port: ${AI_SERVICE_PORT:${PORT_AI:8082}}

spring:
  config:
    import: optional:file:.env[.properties], optional:file:../../.env[.properties], optional:file:../backend/.env[.properties]
  application:
    name: career-os-ai-service

gemini:
  api:
    key: ${GEMINI_API_KEY:}
    model: ${GEMINI_API_MODEL:gemini-2.5-flash}

logging:
  level:
    com.careeros.ai: DEBUG
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  endpoint:
    health:
      show-details: always
